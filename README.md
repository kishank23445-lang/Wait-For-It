# Wait For It!

A dependency-free real-time multiplayer reaction game for 2–8 players.

Run it with Node.js:

```powershell
node server.js
```

Open `http://localhost:3000`. To join from another device on the same network, use the computer's local network address with port `3000` (for example, `http://192.168.x.x:3000`).

